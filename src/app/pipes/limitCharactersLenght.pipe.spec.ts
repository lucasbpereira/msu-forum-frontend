/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { LimitCharactersLenghtPipe } from './limitCharactersLenght.pipe';

describe('Pipe: LimitCharactersLenghte', () => {
  it('create an instance', () => {
    let pipe = new LimitCharactersLenghtPipe();
    expect(pipe).toBeTruthy();
  });
});
